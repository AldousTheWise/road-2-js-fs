/* =====================================
    
    taskItem.js
    Componente que genera el markup de
    una tarea
   ===================================== */

export function TaskItemComponent(task) {
  return /*html*/ `
        <!-- En progreso -->
    `;
}
